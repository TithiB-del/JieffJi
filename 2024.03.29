class Solution {
    public boolean isEularCircuitExist(int v, ArrayList<ArrayList<Integer>> adj) {
        int even = 0;
	    for(int i = 0; i < v; i++) {
	        if(adj.get(i).size() % 2 == 0)
	            even++;
	    }
	    
	    return even == v;
    }
}
